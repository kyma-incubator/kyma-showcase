apiVersion: eventing.kyma-project.io/v1alpha1
kind: Subscription
metadata:
  name: logo-processing-subscription
spec:
  filter:
    filters:
    - eventSource:
        property: source
        type: exact
        value: ""
      eventType:
        property: type
        type: exact
        value: sap.kyma.custom.showcase.logo.processing.v1
  sink: http://logo-processing.default.svc.cluster.local